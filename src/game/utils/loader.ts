// const loader = document.querySelector('.loader') as HTMLElement | null;
// const loaderContainer = document.querySelector('.loader .loader-container') as HTMLElement | null;

export function loading(_thingToLoad: string | null) {
  // if (!loaderContainer) return;

  // if (loader) {
  //   if (thingToLoad) {
  //     loader.style.display = 'block';
  //   } else {
  //     loader.style.display = 'none';
  //   }
  // }
  // loaderContainer.textContent = thingToLoad;
}
